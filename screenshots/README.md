# Screenshots

Real app assets go here. The root `README.md` references these paths — drop the
actual images in and the placeholders will render:

- `detach-hero.png` — hero shot (app blocking a social feed). Used at the top of the root README.
- `blocking-flow.png` — the block/unblock flow (optional).
- `nfc-card.png` — the $9.99 NFC card / a tap-to-lock moment (optional).
- `detach-on-tap.png` — the finished Shortcuts NFC automation (referenced by `../shortcuts/README.md`).

Suggested: 1280×640 or larger PNG/JPG, or an animated GIF for the hero. Keep
file sizes reasonable so the README loads fast.

> These are placeholders. Until real images are added, the image references in
> the READMEs will show alt text instead of a picture.
